import javax.swing.*;
import java.awt.*;

public class Display extends JPanel {
    int height;
    int width; // make a 2d array of rectangles and change color dependant of the
    public Display() {
        setSize(new Dimension(640, 320));
        setBackground(Color.BLACK);
    }

}
